# EduForge 架构方案

> 开源教育 AI 引擎 · 让每个学生都有 AI 老师

---

## 一、产品定位

**EduForge 是教育界的 OpenClaw。**

不是一个固定功能的教学工具，而是一个**开源、插件化的教育 AI 平台**。核心引擎小而精，所有教学功能通过插件实现，教师、开发者和社区可以共建教育生态。

### 核心理念

| 理念 | 说明 |
|------|------|
| 插件化 | 核心只管基础设施，教学能力全部插件化 |
| AI 原生 | AI 不是附加功能，是内置于核心的基础能力 |
| 开源可控 | 学校可私有部署，数据不出校 |
| 社区驱动 | 插件市场 + 教师社区，共建教育生态 |

### 与 OpenClaw 的对应关系

```
OpenClaw                          EduForge
─────────                         ─────────
核心 Agent 引擎        →          核心教学引擎（用户、组织、AI 网关）
Skills 技能系统        →          教学插件系统
├── weather skill      →          ├── AI 批改插件
├── coding agent       →          ├── 错题本插件
├── browser control    →          ├── 学情分析插件
└── ClawhHub           →          └── EduForge Plugin Hub

Channel 渠道           →          多端适配
├── Telegram           →          ├── Web 端
├── Discord            →          ├── 微信小程序
└── Feishu             →          └── 钉钉/飞书集成

自托管 / 数据自控      →          学校私有部署 / 数据不出校
开源社区生态           →          教师 + 开发者共建
```

### 一句话

**OpenClaw 让每个人都有 AI 助手，EduForge 让每个学生都有 AI 老师。**

---

## 二、目标用户

| 用户 | 场景 |
|------|------|
| 独立教师 | 个人使用，出题批改，无需管理功能 |
| 学校 / 培训机构 | 统一部署，管理员配置，多教师多班级 |
| 开发者 | 开发教学插件，贡献到社区 |
| 学生 | 完成作业、自主学习、AI 辅导 |
| 家长 | 查看学情报告（通过微信小程序） |

---

## 三、技术架构

### 技术栈

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 后端引擎 | Fastify (Node.js) | 高性能、插件体系成熟、TypeScript 支持好 |
| 前端 | Next.js + React | 你熟悉、生态好、SSR 支持 |
| 数据库 | PostgreSQL | 开源、信创兼容、性能强 |
| ORM | Prisma | 类型安全、迁移工具好 |
| 缓存 | Redis（可选） | 会话、限流、队列 |
| 部署 | Docker Compose | 一键部署，学校 IT 友好 |

### 项目结构

```
eduforge/
├── packages/
│   ├── core/                    # 核心引擎
│   │   ├── auth/                # 用户认证（JWT + Session）
│   │   ├── rbac/                # 角色权限（ADMIN / TEACHER / STUDENT / PARENT）
│   │   ├── org/                 # 组织结构（学校、年级、班级、学科）
│   │   ├── plugin-engine/       # 插件系统（加载、注册、沙箱、生命周期）
│   │   ├── event-bus/           # 事件总线（插件间通信）
│   │   ├── ai-gateway/          # AI 统一网关（多模型适配）
│   │   └── storage/             # 文件存储抽象
│   ├── sdk/                     # 插件开发 SDK + CLI 工具
│   └── shared/                  # 前后端共享类型定义
├── plugins/                     # 官方内置插件
│   ├── question-bank/           # 题库管理
│   ├── homework/                # 作业系统
│   ├── ai-grading/              # AI 批改
│   ├── mistake-book/            # 错题本
│   ├── analytics/               # 学情分析
│   └── parent-portal/           # 家长端
├── apps/
│   └── web/                     # Next.js 前端（统一）
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
└── docs/                        # 文档
```

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Next.js)                     │
│   统一 UI，通过 API 检测插件启用状态，动态显示功能      │
└──────────────────────┬──────────────────────────────┘
                       │ API
┌──────────────────────▼──────────────────────────────┐
│                  核心引擎 (Fastify)                    │
│  ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐ ┌────────┐  │
│  │ Auth │ │ RBAC │ │  Org   │ │Event │ │   AI   │  │
│  │ 认证 │ │ 权限 │ │ 组织   │ │ Bus  │ │Gateway │  │
│  └──────┘ └──────┘ └────────┘ └──┬───┘ └────┬───┘  │
│                                  │          │       │
│              ┌───────────────────▼──────────▼───┐   │
│              │         插件引擎 (Plugin Engine)    │   │
│              └───────────────────┬──────────────┘   │
└──────────────────────────────────┼──────────────────┘
                                   │
       ┌───────────┬───────────┬───┴───────┬───────────┐
  ┌────▼───┐ ┌─────▼────┐ ┌───▼────┐ ┌────▼───┐ ┌─────▼────┐
  │  题库  │ │   作业   │ │AI 批改 │ │ 错题本 │ │ 学情分析 │
  │ 插件   │ │   插件   │ │  插件  │ │  插件  │ │   插件   │
  └────────┘ └──────────┘ └────────┘ └────────┘ └──────────┘
                          社区插件...
```

---

## 四、核心引擎详细设计

核心引擎只包含平台运行的**最基础能力**，不包含任何教学功能。

### 4.1 用户认证 (Auth)

- JWT + HttpOnly Cookie 双模式
- 支持邮箱密码、手机验证码登录
- 预留 OAuth（微信登录）扩展点
- 多设备会话管理

### 4.2 角色权限 (RBAC)

```
角色层级：
SUPER_ADMIN  → 平台超级管理员
ADMIN        → 学校管理员
TEACHER      → 教师
STUDENT      → 学生
PARENT       → 家长（未来）

权限粒度：
- 资源级：能否访问某个模块
- 操作级：能否执行增删改查
- 数据级：只能看到自己班级的数据
```

### 4.3 组织结构 (Org)

```
核心数据模型：
School（学校）
  └── Grade（年级）
       └── Class（班级）
            ├── Teacher（教师）→ 关联学科
            └── Student（学生）

Subject（学科）
StudyLevel（学段：初中/高中）
```

### 4.4 AI 网关 (AI Gateway)

**这是 EduForge 区别于所有传统教育平台的核心。**

```typescript
// 插件不直接调模型，统一通过 AI Gateway
const result = await ctx.ai.complete({
  task: 'grading',           // 任务类型（批改/辅导/出题/总结...）
  prompt: '...',             // 提示词
  input: submission,         // 输入数据
  options: {
    model: 'auto',           // auto = 系统根据任务选最优模型
    temperature: 0.3,
  }
});
```

功能：
- **多模型适配**：OpenAI / 通义千问 / DeepSeek / 文心一言 / Ollama（本地）
- **用量统计**：按学校、按学生统计 Token 用量
- **限额管理**：免费额度 + 超额策略
- **Prompt 模板**：内置教育场景提示词模板，插件可注册自定义模板
- **降级策略**：主模型不可用时自动切换备用模型

### 4.5 插件引擎 (Plugin Engine)

```typescript
// 插件生命周期
interface EduPlugin {
  name: string;                    // 插件唯一标识
  version: string;                 // 版本号
  dependencies?: string[];         // 依赖的其他插件
  
  onInstall?(ctx: PluginContext): Promise<void>;   // 安装时（建表等）
  onInit?(ctx: PluginContext): Promise<void>;      // 每次启动时
  onDestroy?(ctx: PluginContext): Promise<void>;   // 卸载时
}

// 插件能力
interface PluginContext {
  // 注册 API 路由
  registerRoute(method, path, handler): void;
  
  // 声明数据模型（自动建表）
  registerModel(schema): void;
  
  // 监听事件
  events: {
    on(event: string, handler): void;
    emit(event: string, data): void;
  };
  
  // 调用 AI
  ai: AIGateway;
  
  // 访问核心数据（只读）
  api: CoreAPI;
  
  // 插件自己的数据库操作
  db: PluginDatabase;
  
  // 日志
  logger: Logger;
}
```

### 4.6 事件总线 (Event Bus)

插件间通过事件解耦通信：

```
核心事件：
├── user:created / user:updated
├── class:student_added / class:student_removed
└── system:plugin_installed / system:plugin_removed

插件注册的事件：
├── homework:assigned        → 教师布置了作业
├── homework:submitted       → 学生提交了作业
├── grading:completed        → AI 批改完成
├── mistake:recorded         → 错题被记录
└── analytics:report_ready   → 学情报告生成完毕
```

---

## 五、插件分级

### 核心（不可卸载）

| 模块 | 说明 |
|------|------|
| Auth | 用户认证 |
| RBAC | 角色权限 |
| Org | 组织结构（学校、班级、年级、学科） |
| AI Gateway | AI 统一网关 |
| Plugin Engine | 插件管理 |
| Settings | 系统设置 |

### 官方内置插件（默认启用，可禁用）

| 插件 | 功能 | 依赖 |
|------|------|------|
| question-bank | 题库管理、知识点体系、教材版本 | 无 |
| homework | 作业布置、提交、截止时间 | question-bank |
| ai-grading | AI 自动批改、评分、解析 | homework, AI Gateway |
| mistake-book | 错题自动收集、复习计划 | homework |
| analytics | 学情分析、知识点掌握度、报表 | homework |

### 社区插件（按需安装）

| 插件 | 功能 |
|------|------|
| vocab-trainer | 背单词（记忆曲线 + AI 例句） |
| reading-club | 名著阅读（打卡 + AI 读后感点评） |
| math-olympiad | 奥数训练（竞赛题库 + 分级挑战） |
| essay-grading | 作文批改（AI 评分 + 修改建议） |
| oral-assessment | 口语评测（语音识别 + 发音打分） |
| parent-portal | 家长端（学情推送、家校沟通） |
| gamification | 积分激励（任务积分、排行榜） |
| ai-tutor | AI 学伴（个性化问答辅导） |
| lesson-planner | 备课助手（AI 生成教案） |

---

## 六、插件开发示例

### AI 批改插件

```typescript
import { EduPlugin, PluginContext } from '@eduforge/sdk';

export default class AIGradingPlugin implements EduPlugin {
  name = 'ai-grading';
  version = '1.0.0';
  dependencies = ['homework'];

  async onInit(ctx: PluginContext) {
    // 注册 API
    ctx.registerRoute('POST', '/grading/:submissionId', this.gradeSubmission);
    ctx.registerRoute('GET', '/grading/:submissionId/result', this.getResult);

    // 监听作业提交事件，自动触发批改
    ctx.events.on('homework:submitted', async (submission) => {
      const questions = await ctx.api.homework.getQuestions(submission.homeworkId);
      
      const result = await ctx.ai.complete({
        task: 'grading',
        input: {
          questions,
          answers: submission.answers,
        },
      });

      await ctx.db.save('grading_results', {
        submissionId: submission.id,
        score: result.score,
        details: result.details,
        feedback: result.feedback,
      });

      // 通知其他插件
      ctx.events.emit('grading:completed', {
        submissionId: submission.id,
        studentId: submission.studentId,
        score: result.score,
        wrongQuestions: result.wrongQuestions,
      });
    });
  }
}
```

### 背单词插件（社区开发示例）

```typescript
import { EduPlugin, PluginContext } from '@eduforge/sdk';

export default class VocabTrainerPlugin implements EduPlugin {
  name = 'vocab-trainer';
  version = '1.0.0';

  async onInstall(ctx: PluginContext) {
    // 声明数据模型
    ctx.registerModel({
      name: 'vocab_words',
      fields: {
        word: 'string',
        meaning: 'string',
        level: 'integer',
        category: 'string',
      }
    });
    
    ctx.registerModel({
      name: 'vocab_progress',
      fields: {
        studentId: 'string',
        wordId: 'string',
        familiarity: 'float',    // 0-1 熟悉度
        nextReview: 'datetime',   // 下次复习时间（记忆曲线）
        reviewCount: 'integer',
      }
    });
  }

  async onInit(ctx: PluginContext) {
    ctx.registerRoute('GET', '/vocab/today', this.getTodayWords);
    ctx.registerRoute('POST', '/vocab/review', this.submitReview);
    ctx.registerRoute('GET', '/vocab/stats', this.getStats);
  }
}
```

---

## 七、前端设计

### 原则：前端统一，不做前端插件化

- 前端是一个完整的 Next.js 应用
- 通过 API 检测已启用的插件，动态显示/隐藏功能模块
- 官方插件的 UI 预置在前端代码中
- 第三方插件如需 UI，提供 JSON Schema，前端统一渲染表单和列表

### 页面结构

```
/ (首页/Landing)
/login
/register

/dashboard (根据角色显示不同内容)
  /dashboard/assignments      ← homework 插件
  /dashboard/questions        ← question-bank 插件
  /dashboard/classes          ← 核心
  /dashboard/analytics        ← analytics 插件
  /dashboard/mistakes         ← mistake-book 插件
  /dashboard/vocab            ← vocab-trainer 插件（社区）
  /dashboard/settings         ← 核心

/admin (管理后台)
  /admin/users               ← 核心
  /admin/plugins             ← 插件管理页面（安装/卸载/配置）
  /admin/ai                  ← AI Gateway 配置
  /admin/settings            ← 系统设置

/student (学生端)
  /student/assignments       ← homework 插件
  /student/mistakes          ← mistake-book 插件
  /student/progress          ← analytics 插件
  /student/ai-tutor          ← ai-tutor 插件（社区）
```

---

## 八、数据库设计

### 核心表

```sql
-- 用户
users (id, email, phone, name, password_hash, role, status, ...)

-- 组织
schools (id, name, code, address, ...)
grades (id, school_id, name, code, level, order, ...)
classes (id, grade_id, name, academic_year, ...)
subjects (id, name, code, color, icon, ...)

-- 关联
class_students (class_id, student_id)
class_teachers (class_id, teacher_id, subject_id)

-- 插件
plugins (id, name, version, status, config, installed_at, ...)

-- AI
ai_configs (id, school_id, provider, model, api_key, ...)
ai_usage_logs (id, school_id, user_id, plugin, tokens, cost, ...)

-- 系统
settings (key, value, scope, ...)
```

### 插件表

插件通过 SDK 声明数据模型，核心引擎自动创建表，统一加前缀：

```sql
-- question-bank 插件
plugin_qb_questions (...)
plugin_qb_knowledge_points (...)

-- homework 插件
plugin_hw_assignments (...)
plugin_hw_submissions (...)

-- ai-grading 插件
plugin_ag_results (...)
```

**插件只能读核心表（通过 API），不能直接写核心表。**

---

## 九、信创兼容

| 要求 | EduForge 方案 | 兼容性 |
|------|-------------|--------|
| 操作系统 | Docker 部署 | ✅ 可跑在统信 UOS、麒麟 |
| 数据库 | PostgreSQL | ✅ 信创目录认可 |
| AI 模型 | 通义千问/DeepSeek/文心一言 | ✅ 国产大模型 |
| 芯片架构 | Node.js | ✅ ARM（鲲鹏/飞腾）可运行 |
| 浏览器 | Web 标准 | ✅ 不依赖特定浏览器 |

---

## 十、盈利模式

| 层级 | 内容 | 定价 |
|------|------|------|
| 🆓 开源版 | 核心引擎 + 官方内置插件 | 免费 |
| ☁️ 托管版 | 免部署托管服务 | 年费制 |
| 🤖 AI 服务 | 统一 AI 接口代理 | 按用量付费 |
| 💎 高级插件 | 高级学情分析、家长端等 | 订阅制 |
| 🏪 插件市场 | 第三方插件分发，平台抽成 | 分成 |

---

## 十一、开发路线

### Phase 1：核心引擎（第 1 个月）

- [ ] 项目初始化（Monorepo + TypeScript）
- [ ] 核心引擎搭建（Fastify + Prisma）
- [ ] 用户认证 + RBAC
- [ ] 组织结构（学校、年级、班级）
- [ ] 插件引擎（加载、注册、生命周期、事件总线）
- [ ] AI Gateway（多模型适配、统一接口）
- [ ] 前端框架搭建（Next.js + 基础布局）

### Phase 2：官方插件（第 2 个月）

- [ ] 题库插件（题目 CRUD、知识点、教材版本）
- [ ] 作业插件（布置、提交、截止时间）
- [ ] AI 批改插件（自动批改、评分、反馈）
- [ ] 前端页面（教师端 + 学生端基础 UI）
- [ ] 管理后台（用户管理、插件管理、AI 配置）

### Phase 3：完善 + SDK（第 3 个月）

- [ ] 错题本插件
- [ ] 学情分析插件
- [ ] 插件 SDK 文档 + 开发指南
- [ ] Docker 一键部署
- [ ] GitHub 开源发布（README、Demo、截图）

### Phase 4：生态建设（3-6 个月）

- [ ] 插件市场上线
- [ ] 社区运营（开发者 + 教师）
- [ ] 托管版上线
- [ ] 找到第一批付费用户

### Phase 5：长期愿景（6 个月+）

- [ ] AI 学伴（每个学生的个性化 AI 老师）
- [ ] 微信小程序（家长端）
- [ ] 多租户 SaaS
- [ ] 国际化

---

## 十二、竞品对比

| 维度 | 科大讯飞 | 七天学堂 | Moodle | **EduForge** |
|------|---------|---------|--------|-------------|
| 开源 | ❌ | ❌ | ✅ | ✅ |
| AI 原生 | 部分 | ❌ | ❌ | ✅ |
| 插件化 | ❌ | ❌ | ✅（但老旧） | ✅ |
| 中国 K12 适配 | ✅ | ✅ | ❌ | ✅ |
| 部署成本 | 高 | 中 | 中 | 低 |
| 定制性 | 低 | 低 | 中 | 高 |
| 价格 | 贵 | 中 | 免费 | 免费 |

**EduForge 的独特位置：开源 + AI 原生 + 插件化 + 中国 K12，目前无直接竞品。**

---

*文档版本：v1.0*
*创建日期：2026-02-14*
*作者：EduForge Team*
